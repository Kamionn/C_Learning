#include <stdio.h>
#include <stdlib.h>
#include <math.h>


int main()
{
    int boucle = 10;

    while (boucle != 0)
    {
         printf("Nombre de boucle : %d\n", boucle);
         boucle --; // Incrémentation
         boucle ++; // Infinis
    }
}
